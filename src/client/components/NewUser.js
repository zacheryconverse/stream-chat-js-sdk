import React from 'react'

export default function NewUser({ chatClient }) {
  // static contextType = ClientContext;
  // console.log(context);
  return (
    <div className="New-user">
      Join a channel to start the fun
    </div>
  )
};
